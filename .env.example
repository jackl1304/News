# Quellen-Spezifikationen (module|Class|arg1|arg2;…)
SOURCE_SPECS=

# SQLite-Datenbank (lokal)
DB_PATH=./data/news.db

# Gmail SMTP (App-Passwort nutzen)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=dein.username@gmail.com
SMTP_PASS=DEIN_APP_PASSWORT

# Empfänger (Komma-getrennt)
RECIPIENTS=mail1@example.com,mail2@example.com

# Scheduler-Intervall (in Minuten)
POLL_INTERVAL_MINUTES=15

# Wöchentlicher Cron (z. B. Montag 08:00)
WEEKLY_CRON=0 8 * * MON
